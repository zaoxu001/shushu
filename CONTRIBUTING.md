# 参与指南

本项目的目标是把术数中可计算的部分，实现得准确且可核对。因此贡献的价值不在代码量，
而在于是否提供了可以核对的依据。

## 贡献方式

### 一、指出口径分歧

术数各派算法不同是常态。同一张命盘，依《穷通宝鉴》与依《金不换大运》取用，
结论可能不一致，而两者各有出处。这类情况并非孰对孰错，而是需要明确本项目采用哪一种口径。

请创建「口径分歧」issue，提供以下三项：

1. 分歧所在的具体函数或规则
2. 可复现的实例：四柱、性别，本项目的输出，以及你认为正确的输出
3. 依据：典籍名称、卷次、原文；或某一流派成文的口诀及其出处

三项齐备即可进入核对。核对通过后，或调整默认口径，或作为可选口径与现有实现并存。

### 二、提供实例

一条规则在典籍中成立，不等于在所有命盘上都能落实。
带出处的真实命例（古籍所载命例、有通行断法的名人命盘）比论述更有价值。

### 三、提交代码

修改计算逻辑前，请先创建 issue 说明拟改内容与依据，待回应后再动手。
未经讨论直接提交的大型 PR 通常需要返工,并非不欢迎，而是口径未确定之前，
实现得再完善也无法合入。

## 三条基本要求

**一、修改计算逻辑必须提供依据。**
依据指典籍名称、卷次与原文，或某一流派成文的口诀及其来源。
「按经验应当如此」一类的表述无法核对，因而无法作为合入依据；
若来自师承，请将相应口诀完整写出,写出来即可核对。

**二、不接受未经核实的 AI 生成内容。**
本项目不反对使用生成式工具辅助编写测试、整理格式或检索资料。
但术数的正误需由典籍与实例判定，而生成式模型的典型失效方式恰恰是产出形似可信的出处：
书名、卷次、行文均无破绽，唯独内容为虚构。此类内容一旦合入，
本项目"每一条结论都可追溯至出处"的前提即不再成立。
因此提交前请自行核对原文。

**三、不引入新的第三方依赖。**
目前仅依赖 `lunar-python` 用于历法计算。新增任何依赖都需在 PR 中说明其不可替代性。
本项目定位为供他人调用的基础库，每增加一项依赖，下游都需相应承担其风险。

## 本地开发

```bash
git clone https://github.com/zaoxu001/shushu.git
cd shushu
pip install -e . pytest
pytest -q                 # 确认全部通过后再开始修改
```

提交前：

```bash
pytest -q
```

涉及口径调整的改动，需补充一条锁定该口径的测试，并使用真实四柱而非构造数据。
测试的作用不在于证明本次修改正确，而在于确保后续修改不会将其悄然回退。

## 合入流程

外部贡献通过 fork 与 Pull Request 提交。主分支启用保护，所有改动均需经 PR 与审阅。
审阅关注两点：依据是否成立，测试是否足以锁定该行为。
工程风格方面的意见会一并给出，但不作为合入的阻碍条件。

## 分层与各层规则

修改前请先确认所涉层次,本项目分为四层，各层的改动标准不同：

| 层 | 内容 | 规则 |
|---|---|---|
| `calendar` | 历法、节气、真太阳时 | 属天文计算，正误有客观标准，不存在口径分歧 |
| `core` | 五行、干支、藏干、刑冲合害 | 基础常识，其上所有逻辑均依赖于此，改动须格外审慎 |
| `bazi` | 旺衰、调候、格局、取用、引动 | 口径分歧主要集中于此层，亦最欢迎讨论 |
| `data` | 由典籍编码而成的数据表 | 修改任一条目均须附原文 |

全部实现为纯函数：不读取系统时间，不访问网络与文件，相同输入恒得相同输出。
此约束不可突破,一旦突破，既无法测试，也无法验证。
