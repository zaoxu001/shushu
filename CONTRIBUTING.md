# 参与

这个包做的是一件具体的事：把术数里能算的那部分，算准、算得可核对。
所以这里最欢迎的不是代码量，是**依据**。

## 三种参与

### 一、指出口径分歧

各派算法不同是常态。同一张盘，穷通与金不换给出不同的喜忌，两边都有出处。
这种情况不是谁错了，是需要把口径说清楚。

开一个「口径分歧」issue，把三样东西写全：

1. 哪一处算得与你不同
2. 一个能复现的例子（四柱、性别，这里算出什么、应当算出什么）
3. **依据**,典籍名、卷次、原文，或某一派成文的口诀与出处

三样齐了就能核对。核对通过的，要么改默认口径，要么作为可选口径并存。

### 二、提供实例

一条规则在书上成立，不等于在所有盘上都落得住。
带出处的真实案例（古籍医案式的命例、名人盘的通行断法）比一百句议论有用。

### 三、代码

先开 issue 说清要改什么、依据是什么，得到回应再动手。
直接甩一个大 PR 过来，多半要推倒重来,不是不领情，是口径没谈拢之前，代码写得再好也没法合。

## 规矩

**动了计算逻辑，必须给出处。** 这是这个项目唯一的硬规矩。
「我觉得应该这样」「我师父就是这么教的」不能作为合入依据,前者无法核对，
后者请把师承的那套口诀写出来，写出来就能核对了。

**未经验证的 AI 生成内容不收。** 不是反对用工具,写测试、改格式、查资料，随便用。
但术数的对错要靠典籍与实例来定，而生成式模型最擅长的恰恰是编出看着像出处的东西：
书名对、卷次对、语气对，只有内容是假的。这种东西一旦混进来，
整个项目"每一条都追得到出处"的前提就塌了。所以提交前请自己核一遍原文。

**不引新依赖。** 现在只依赖 `lunar-python`（历法）。
再加任何一个都要在 PR 里说明为什么非它不可。这个包是给别人当地基用的，
地基上每多一块砖，用它的人就多担一份风险。

## 动手

```bash
git clone https://github.com/zaoxu001/shushu.git
cd shushu
pip install -e . pytest
pytest -q                 # 全绿再开始
```

改完：

```bash
pytest -q
```

改了口径的，**补一条锁住这条口径的测试**,拿真实四柱，不要编数据。
测试的作用不是证明你写对了，是保证下一个人改的时候不会悄悄改回去。

## 合入流程

外部贡献走 fork + Pull Request。仓库主分支受保护，任何改动都要经过 PR 与审阅。
审的是两件事：依据站不站得住、测试锁不锁得住。工程风格上的意见会说，但不拦路。

## 分层

改之前先看清楚改的是哪一层,这个包分四层，各层的规矩不同：

| 层 | 内容 | 规矩 |
|---|---|---|
| `calendar` | 历法、节气、真太阳时 | 天文计算，对错有客观标准，不存在口径之争 |
| `core` | 五行、干支、藏干、刑冲合害 | 基本常识，改动须极慎重,上面所有东西都压在这里 |
| `bazi` | 旺衰、调候、格局、取用、引动 | 口径之争主要在这一层，也是最欢迎讨论的地方 |
| `data` | 典籍编码的表 | 改一个格子就要贴出原文 |

全部是纯函数：不读系统时间、不碰网络与文件，同一输入永远同一输出。
这条不能破,破了就没法测，也没法让人信。
