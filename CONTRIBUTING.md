# 参与指南

本项目致力于把术数中可计算的部分实现得准确、可核对。欢迎各位专业人士提供建议与案例佐证，
共同建设传统文化。

## 关于口径分歧

术数各派算法不同是常态。同一张命盘，依《穷通宝鉴》与依《金不换大运》取用，结论可能不一致，
而两者各有出处。这类情况通常不是对错之分，而是需要明确本项目采用何种口径。

遇到与本项目实现不一致的地方，请在 issue 中准确描述：

1. 分歧所在的函数或规则
2. 可复现的实例：四柱、性别，本项目的输出，以及你认为正确的输出
3. 所依流派的算法及其出处

经核对确认后，会合入计算逻辑，或作为可选口径与现有实现并存。

## 关于案例

一条规则在典籍中成立，不等于在所有命盘上都能落实。带出处的真实命例
（古籍所载命例、有通行断法的名人命盘）对校验实现很有帮助，欢迎提供。

## 关于代码

修改计算逻辑前，建议先开 issue 说明拟改内容与依据，讨论后再动手,口径确定之后，
实现层面的工作会顺利很多。

外部贡献通过 fork 与 Pull Request 提交。主分支启用保护，改动需经 PR 与审阅。

## 唯一的硬性规定

**禁止不经验证的 AI 灌水。**

不反对使用生成式工具辅助编写测试、整理格式或检索资料。但术数的正误需由典籍与实例判定，
而生成式模型常见的失效方式恰是产出形似可信、实则虚构的出处。提交前请自行核对原文。

## 本地开发

```bash
git clone https://github.com/zaoxu001/shushu.git
cd shushu
pip install -e . pytest
pytest -q
```

涉及口径调整的改动，建议补充一条锁定该口径的测试，并使用真实四柱,
这样后续修改时不会将其悄然回退。

目前仅依赖 `lunar-python` 用于历法计算。本项目定位为供他人调用的基础库，
新增依赖会传导至所有下游，如确有必要请在 PR 中一并说明。

## 分层

本项目分为四层，修改前请先确认所涉层次：

| 层 | 内容 | 说明 |
|---|---|---|
| `calendar` | 历法、节气、真太阳时 | 属天文计算，正误有客观标准，不存在口径分歧 |
| `core` | 五行、干支、藏干、刑冲合害 | 基础常识，其上所有逻辑均依赖于此，改动宜审慎 |
| `bazi` | 旺衰、调候、格局、取用、引动 | 口径分歧主要集中于此层，也最欢迎讨论 |
| `data` | 由典籍编码而成的数据表 | 修改条目时请附原文 |

全部实现为纯函数：不读取系统时间，不访问网络与文件，相同输入恒得相同输出。
这是可测试、可复现的前提。
