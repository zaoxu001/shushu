# 安全问题怎么报

这个包只做本地计算：不联网、不读写文件、不执行外部命令，也不碰任何凭据。
所以能构成安全问题的面很窄，大致只有两类：

- 处理畸形输入时崩溃或陷入死循环（可被用来打瘫调用方的服务）
- 依赖链上的已知漏洞

**请不要开公开 issue。** 用 GitHub 的私密报告通道：
仓库页 → Security → Report a vulnerability。

我会在七天内回复。修好之后会在 release note 里致谢，你不希望署名请说一声。
